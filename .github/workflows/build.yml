name: building image
run-name: ${{ github.head_ref }} 
on: 
  push:
    branches: [ "main" ]
jobs:
  build-image:
    runs-on: local-system
    steps:
      - run: echo ${{ secrets.MESSAGE }}
      - run: echo -e "GITHUB_ref:${{github.ref}}\nGITHUB_API_URL:${{github.api_url}}"
      # - run: docker build -t flask:${{github.ref}} -f docker/Dockerfile