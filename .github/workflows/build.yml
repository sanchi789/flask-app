name: building image
run-name: ${{ github.head_ref }} 
on: 
  push:
    branches: [ "main" ]
jobs:
  build-image:
    runs-on: local-system
    steps:
      - run: echo $MESSAGE
      - run: docker build -t flask:$GITHUB_HEAD_REF -f docker/Dockerfile